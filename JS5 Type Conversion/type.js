// Type conversion - changing the data type of the value into another (String, Number & Boolean)

// Strings to Numbers // Numbers to Booleans // Booelans to Strings // 

/*let age = window.prompt("What's your age ?");

age+=1; //this will consider age as 251 when age is given 25 in prompt

console.log(age);
*/

/*let age = window.prompt("What's your age ?");

age = Number(age);
age+=1;

console.log(age, typeof(age)); //Answer will be 26

*/

let x = "Elephant";
let y = "Lion";       
let z = "Vegetable"; //if z =" " then boolean will be false for empty gaps

x = Number (x);
y = String (y);
z = Boolean (z);

console.log(x, typeof x);
console.log(y, typeof y);
console.log(z, typeof z);



